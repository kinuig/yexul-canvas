# YexuL Canvas · 快速开始

**YexuL Canvas** 是一款在本地运行的「无限画布 AI 生图 + 多窗口 AI 对话」工具：生图区 / 画布分区 / MJ 生图区 / GPT 对话窗口 / 历史记录 / 常用提示词库 / Skill 系统，全部数据保存在本机（`data\` 与 `cache\`），不经过任何第三方服务器（除你配置的 AI API 平台外）。

---

## 一、Windows 安装运行

1. 解压下载的 zip 到任意目录；
2. **双击 `启动画布.bat`**（弹出一个黑色命令行窗口，保持开启即为运行中）；
3. 浏览器会自动打开 `http://127.0.0.1:172`（若端口被占用会自动顺延 173/174…，以窗口里打印的地址为准）；
4. 首次使用：点右上角 **⚙ 设置** → 填入 API 提供商（名称 / Base URL / API Key）→ 点 **「🔌 测试连接并拉取模型」** → 在弹出窗口里**勾选**要用的生图/对话模型 → 保存；
5. 开始创作：画布上「＋ 新建生图区」→ 输入提示词 → **✨ 生成图片**；点 **💬 新建对话** 打开聊天窗口。

> Windows 版已内置便携 Node 运行时，**无需安装任何环境**。

## 二、macOS 安装运行

1. 解压 zip 到任意目录（例如 `~/yexul-canvas`）；
2. **双击 `启动画布.command`**（首次若提示无法打开：右键该文件 → 打开 → 打开；若弹出「**允许传入连接**」防火墙提示，点**允许**，仅本机访问）；
3. 浏览器会自动打开 `http://127.0.0.1:172`，后续配置与 Windows 相同。

> macOS 版已内置便携 Node 运行时（Apple 芯片与 Intel 芯片均支持），**无需安装任何环境**；若删除了 `node` 文件夹，则需系统装有 Node.js 18+（`brew install node`）。

## 三、日常使用速览

| 想做什么 | 怎么做 |
| --- | --- |
| 文生图 / 图生图 / 编辑 | 「＋ 新建生图区」，模型+提示词+比例分辨率+数量，✨ 生成 |
| 多张并行 | 生图区「数量」选 N，出一张实时回传一张，可点「⏹ 停止」中断 |
| MJ 生图 | 「🎨 MJ生图」按钮，Imagine/Blend + U/V 放大变体（需在设置里配 MJ 接口地址） |
| AI 对话 | 「💬 新建对话」，多窗口、流式、附图、Markdown |
| 加载 Skill | 对话窗口模型行下方按钮，点击加载/卸载（如「Midjourney提示词优化」） |
| 自定义 Skill | 把 `.md` 文件放进 `skills\` 目录（`---` frontmatter 写 `name`/`description`），重启生效 |
| 常用提示词 | 生图区「💾 保存当前提示词」；对话窗口「💾 保存消息」 |
| 历史回看 | 顶部「历史」按钮，可一键放回画布 |

## 四、数据与安全

- 所有配置（API 提供商、Key、画布、对话、提示词库、日志）保存在本机 `data\` 目录；
- 生成图 / 上传图缓存在 `cache\` 目录（可随时手动删除）；
- **API Key 只存在你自己的电脑上**，不会上传到任何服务器；
- 端口默认 172，可在 `config.json` 修改。

## 五、常见问题

- **打不开页面**：看命令行窗口里打印的实际地址（端口被占用会自动顺延 173~191；**全部被占用时自动改用系统随机端口**，以窗口里「浏览器地址」一行为准）。也可以用环境变量指定端口：`PORT=8888 ./启动画布.command`（Windows 可先 `set PORT=8888` 再双击 bat）。
- **报 401 / 鉴权失败**：检查 API Key 是否复制完整、是否有余额。
- **报 404**：Base URL 填错或模型名平台已下架。
- **模型下拉为空**：设置 → 提供商 → 🗂 选择模型 → 勾选模型。
- **MJ 报 404/405**：该地址不支持 Midjourney 接口，请在设置「Midjourney API 独立配置」里填支持 MJ 的地址。
- **升级版本**：用新版本文件覆盖旧目录即可（保留 `data\` 与 `cache\`，配置和图片都在）。
- **停止服务**：关闭命令行窗口即可。
