# YexuL Canvas · 无限画布生图（Windows 版）

基于 [gpt-best API 文档](https://gpt-best.apifox.cn/) 的本地无限画布生图应用。
双击启动 → 自动打开浏览器（默认 `http://127.0.0.1:172`），所有生成图与上传图都会**缓存到本地文件夹**。

> 界面与交互（无限画布 / 节点拖拽）参照 Infinite-Canvas 项目的使用习惯设计。

---

## 一、快速开始

**Windows**

1. **双击 `启动画布.bat`**，黑色窗口保持打开即为运行中；
2. 浏览器会自动打开 `http://127.0.0.1:172`；
3. 首次使用：点击右上角 **⚙ 设置**，填入 **Base URL** 与 **API Key**，点「测试连接并拉取模型」，勾选要用的模型并保存；
4. 画布上输入提示词 → 点 **✨ 生成图片**；点 **💬 新建对话** 打开 GPT 聊天窗口。

**macOS**

1. **双击 `启动画布.command`**（首次若提示无法打开：右键 → 打开 → 打开；防火墙提示点「允许」）；
2. 浏览器会自动打开 `http://127.0.0.1:172`，其余步骤与 Windows 相同。

> 环境要求：**Windows 与 macOS 均已内置便携 Node**（`node\node.exe` / `node\macos\`，Apple 芯片与 Intel 芯片均支持），双击即可运行，无需安装任何环境；若删除了 node 文件夹，则需系统装有 Node.js 18+。

## 二、API 提供商（可保存多个，随时切换）

1. 打开 gpt-best 平台工作台（[接口文档](https://gpt-best.apifox.cn/) 有入口）；
2. **Base URL**：工作台页面显示的接口地址，通常就是网站域名（程序会自动补全 `/v1`）；
3. **API Key**：在平台「令牌 / API Keys」页面创建，格式 `sk-xxxx`；
4. **🎨 Midjourney 独立配置**：设置里有单独一栏「MJ 接口地址 + MJ API Key」，**与当前提供商无关**——填好并点「保存设置」后，无论切换到哪个提供商，MJ 生图区都固定走这里填的地址和 Key（留空才回退到当前提供商）；报 404/405 说明该地址不支持 Midjourney 接口。新安装默认为空，需要自行填写支持 MJ 的接口地址；
4. 回到本应用 ⚙ 设置 → 填写名称 / Base URL / API Key → 点 **「🔌 测试连接并拉取模型」**：
   - 连接成功后自动弹出 **「选择要使用的模型」窗口**：生图模型、对话模型**多选勾选**（可全选/全不选），**未勾选的模型不会出现在生图区与对话下拉里**；
   - 生图区模型下拉**只显示你勾选的模型**（内置默认目录已清除），勾选窗口随时可从提供商行的「🗂 选择模型」重新打开；
5. 提供商列表支持：**使用（切换）/ 🗂 选择模型 / 🔄 重新拉取 / ✎ 编辑 / ✕ 删除**，生图和对话默认都走「当前使用」的提供商；配置保存在本机 `data\providers.json`；
6. **兼容性**：支持 gpt-best（comfly）、**NewAPI（docs.newapi.pro，标准 OpenAI 兼容网关）** 等平台的切换——
   - 生图统一走 OpenAI 格式 `/v1/images/generations`；MJ 走 `/mj/submit/imagine` 等标准路径；
   - **comfly 与 NewAPI 的接入手册不完全一样**：comfly 的 Gemini 生图模型走标准 generations 接口；**NewAPI 系平台的 Gemini 生图模型只走原生 `/v1beta/models/{model}:generateContent` 格式**——程序已做自动适配（平台报「not supported model for image generation / only imagen models」时自动改用原生格式重试），两边都能正常出图，无需手动切换；
   - **张数语义**：所选张数 = 最终出图张数；多张时统一按「每次请求 1 张、并发 3」自动补齐（不再把 n 交给平台，GPT 等多张只回 1 张的问题已修复）；
   - **nano-banana 分辨率**：通过 `image_size`（1K/2K/4K）下发，选 2K/4K 即生效（含带渠道前缀的模型，如 `[yu]gemini-…`；实测 4K 出图 5504×3072）；
   - **momo（NewAPI 系）的 GPT 生图**：平台上游通道不稳——报 502（`bad_response_status_code`/`openai_error`）是平台侧问题；**连续重试会触发 429 限流**（NewAPI 手册：请求过频 / 分组负载饱和，默认每 IP 180 次/180 秒），报 429 后请等 1 分钟再试，不要连点；Gemini 通道相对稳定；
   - 平台高峰 503 自动重试（2s/5s/10s 退避）；模型拉取只收录 **Gemini / nano-banana / GPT-image** 生图家族，拉取失败也照常保存并启用提供商；
   - **GRS（grsai.ai）**：该平台**不开放 `/v1/models` 模型拉取接口**——填好 Base URL（域名含 grs 字样）后点「测试连接并拉取模型」，程序会**自动载入官网内置模型清单**（生图 13 个 / 对话 11 个），并在「选择模型」窗口里**默认不勾选**，你勾哪些就用哪些；生图接口走标准 `/v1/images/generations`；
   - **GRS 分辨率规则（实测）**：**nano-banana 系列走 GRS 专用接口 `/v1/api/generate`**，分辨率通过 `imageSize`（1K/2K/4K）参数下发——**普通 nano-banana-2 即可出 4K（实测 4096×4096）**，不再自动切换 -cl/-4k-cl 等贵档位型号；比例通过 `aspectRatio` 下发（支持 auto/1:1/16:9/9:16/4:3/3:4/3:2/2:3/5:4/4:5/21:9 及 1:4/4:1/1:8/8:1）。GPT 生图模型仍按 `size` 出图（选 4K 即 3840×2160）。**建议 Base URL 填 `https://grsaiapi.com/`**（官方 API 域名，稳定；`grsai.dakka.com.cn` 实测不稳且偶发 400）；
   - **GRS 参考图限制（实测）**：平台请求体上限约 32MB（nginx 413），**大参考图必须压缩**——在 GRS 提供商下添加参考图时，程序会**自动把参考图压缩到 2048px 以内（JPEG）**再上传（上传本地图 / 从画布选择 / 生成结果设参考，全部生效）；程序还会在超限前给出友好提示而不是 413 报错；
   - **MJ 建议用 comfly**：comfly 返回「四宫格整图 + U/V 按钮」，图片在平台自己的 CDN（国内可直接下载）；部分 NewAPI 系平台（如 momo）的 MJ 图挂在 discord CDN（国内直连 403 下载失败），且任务不带 U/V 按钮，无法在画布里继续放大/变体。

## 三、功能一览

| 功能 | 说明 |
| --- | --- |
| 🧩 无限画布 | 滚轮缩放（0.08x~4x）、按住空白拖拽平移、空格+拖拽平移（**拖动卡顿已修复**），点右上角「＋ 新建生图区」创建生图区；**画布图片左上角 🔍 点击进入大图模式**（横竖图都完整适配不裁边，滚轮缩放、按住拖动平移、双击复位）；**画布图片节点可点名字后的 ✎ 修改名字**（改完点其他地方自动保存，改名后在选择器里按名字显示，方便调用） |
| 🗂 画布分区 | 顶部页签：**当前所在分区显示为「当前分区」**，右上角「＋ 新建」新建分区，✕ 删除；点其他页签切换分区，点回主分区即**切回大的工作区** |
| 🎨 MJ 生图区 | 顶部「MJ生图」按钮（在新建对话左边）新建 **Midjourney 生图区**：Imagine 文生图 / Blend 图生图，绘图模式（默认/Fast/Turbo/Relax）、**图片通道（默认/转发加速 relay/原图 origin/平台代理 proxy，国内访问建议 relay）**、比例 --ar、版本 --v、质量 --q、Midjourney/Niji 机器人、镜头焦段、光圈、HD，出图后可点 **U/V 等操作按钮**放大变体、reroll；**结果按「任务」分组**（一次跑图的一组卡片，最新置顶，组头带「⤢ 到画布」一键把这组图全部放上画布），四宫格逐张回传、U/V 按钮挂在对应象限图上；风格与生图区一致，选项参照 PS 插件 mj-shengtu |
| 🎨 生图区 | 文生图 / 图生图 / 图像编辑 全流程：模型、提示词、负面提示词、**比例+分辨率**、数量、模式；**点标题旁 ✎ 修改生图区名字**；结果区右上角「🗑 清除结果」清空展示（历史与缓存保留）；**结果按「任务」分组显示**（每组标题显示模型 · 比例/分辨率 · 张数 · 完成时间，最新任务置顶，鼠标悬停看提示词，**组头有「⤢ 到画布」按钮可把这一组图片全部放上画布**）；**多张时出一张实时回传一张**，状态栏显示**进度（已出 x/n · 计时）**，生成中按钮变「⏹ 停止」**可随时中断**（已回传的图保留） |
| 📐 比例 / 尺寸 | 统一比例：1:1 / 2:3 / 3:2 / 3:4 / 4:3 / 4:5 / 5:4 / 9:16 / 16:9 / 1:2 / 2:1 / 9:21 / 21:9 + 自定义；分辨率 1K / 2K / 4K（按长边 1024 / 2048 / 3840 换算，自动适配各模型） |
| ⚙ 高级设置 | Quality（Auto/High/Medium/LOW）、Background（Auto/透明/白/黑）、Format（PNG/JPEG/WEBP）、Moderation（Auto/低/关） |
| 💬 GPT 对话窗口 | **多开**（顶部「新建对话」按钮）、**点标题旁 ✎ 修改窗口名字（失焦自动保存）**、**窗口可自由拖动**（按住标题栏拖动，位置自动记忆）、流式输出、Markdown 渲染、**插入图片（可多选画布中的多张图，缩略图带名字）**问视觉模型、可最小化/调大小，对话自动保存，重开自动恢复；**每条消息（AI 回复和我发出的）悬停右下角有「📋 复制」「❝ 引用」**；**窗口顶部模型下拉与生图区一致：只显示勾选的对话模型**；**默认对话模型 = ⚙ 设置里保存的勾选模型（留空自动用勾选列表第一个，无内置预设）** |
| ⬆ 上传本地图片插件 | 顶部「上传图片」按钮、直接把图片文件**拖进画布**、Ctrl+V 粘贴、拖进生图区参考图区域 |
| 🎯 图生图 | 在生图区添加参考图（本地图片 / 画布上任意图 / 上一次生成结果，缩略图带名字）即可图生图 |
| ✏️ 图像编辑 | 支持编辑的模型（gpt-image-1/2、qwen-image-edit、seededit 等）切换到「编辑」模式 |
| 🕘 历史记录 | 每次生成自动记录，可一键把历史图片放回画布、重新下载 |
| 🛠 任务状态与日志 | 右上角状态栏实时显示**当前任务状态**（空闲 / 生成中·计时 / 完成 / 失败），点击弹出**任务日志**面板：任务记录 + **服务端接入日志**（MJ 提交/轮询/下载全过程，黑色窗口同步打印、`data\logs.jsonl` 落盘），报错不再看不到 |
| 🗂 本地缓存 | 生成图 → `cache\generated\`，上传图 → `cache\uploads\`，顶部「缓存」按钮直接打开文件夹 |

### 尺寸换算规则（比例 + 分辨率 → 实际尺寸）

- 以**长边**为准：1K=1024、2K=2048、4K=3840，短边按比例计算并取整到 16 的倍数；
- 支持自定义尺寸的模型（如 gpt-image-2）→ 直接使用换算尺寸；
- 固定尺寸表模型（flux / recraft / qwen 等）→ 自动选**比例与像素最接近**的一档；
- 比例类模型（nano-banana 家族 / grok / flux-kontext）→ 直接传 `aspect_ratio`；**Nano Banana 家族额外传 `image_size`（1K/2K/4K）控制分辨率**；张数与其他模型一致，多张时并发补齐；
- 比例选「自定义…」→ 手动输入 宽x高。

### 支持的绘图模型（按文档整理，以平台实际为准）

- **GPT 系列**：gpt-image-1 / gpt-image-2 / gpt-4o-image(-vip) / dall-e-3 / sora_image(-vip)
- **Google**：nano-banana / nano-banana-hd / nano-banana-2 / Nano Banana 3.1 Flash（模型名 `gemini-3.1-flash-image-preview`）/ 3.1 Flash-Lite（`gemini-3.1-flash-lite-image`）
- **Flux**：flux / flux-dev / flux-pro / flux-kontext-pro / max / dev
- **通义**：qwen-image / qwen-image-edit / qwen-image-edit-2509
- **豆包·即梦**：seedream-v5-pro / seedream-4.0 / seedream-3.0 / seededit-3.0
- **其他**：grok-4.1/4.2-image、recraftv3(-halloween)

### 对话窗口说明

- **Skill 加载栏（智能体）**：对话窗口顶部有一排 Skill 按钮（位于模型行下方），**点击即加载该 Skill 作为本窗口的系统提示词**（按钮变紫 ✓ 高亮），再点一次卸载；每个窗口可独立加载，状态自动保存。Skill 文件放在程序目录 `skills\` 下（`.md` / `.txt`，可用 `---` frontmatter 写 `name` / `description`），新增文件后重启程序即出现新按钮。当前内置 1 个（提取自 PS 插件 mj-shengtu 的智能体资产，**提示词规则与资产库词库已合并**）：**Midjourney提示词优化**（自然语言 → MJ v8.2 英文提示词；内置专业词汇资产库与 12 个范本，输出自动匹配资产库词汇并做针对性优化，附【资产库词汇】清单与【参数建议】）；
- 聊天走平台 `/v1/chat/completions`（OpenAI 格式）；**窗口顶部的模型下拉与生图区一致：只显示「选择模型」窗口里勾选的对话模型**（未勾选的不出现，想用其他模型去 ⚙ 设置 → 选择模型 勾选即可）；勾选列表为空时显示可手动输入的文本框；
- **对话模型列表与生图区一致：只显示勾选的模型，无内置预设**；⚙ 设置里的「默认对话模型」留空时，新窗口自动用勾选列表的第一个；
- 每条消息气泡**右下角悬停出现「📋 复制」（复制整条内容）和「❝ 引用」（以 `>` 引用格式插入输入框）**，AI 回复和我发出的消息都有；
- **💾 保存消息 / ⭐ 选择常用对话**：在输入框写好内容后点「保存消息」→ 自定义名称存入**常用对话库**（`data\chat-prompts.json`，同名覆盖）；「选择模型」下方点「选择常用对话」→ 点任意一条即可载入输入框；
- **生图区「💾 保存当前提示词」「📂 常用提示词」**：存的是**常用提示词库**（`data\prompts.json`），与对话库**分开保存、互不混用**；
- **插入图片（多图）**：窗口工具条点 🖼 → 弹出选择器，**点击即可多选无限画布中的任意图片**（缩略图显示名字，图片节点重命名后同步显示新名字），点「插入 N 张」；也可以上传新图（上传后可继续多选）；画布上图片节点的悬浮按钮 💬 可一键插入到最近使用的对话窗口；直接拖图片文件进窗口同样支持多张；
- 附加图片后发消息 = 视觉多模态输入（base64 data URI），适用于 gpt-5.6-sol / gpt-4o / gemini / qwen-vl 等视觉模型；
- 每个窗口独立上下文（自动携带最近 40 条消息），窗口状态（名称/模型/消息/位置/大小）保存于 `data\chats.json`。

## 四、目录与数据说明

```
yexul-canvas\
├─ 启动画布.bat        双击运行
├─ server.js           本地服务（零依赖，Node 内置模块实现）
├─ node\node.exe       便携版 Node 运行时（免安装）
├─ config.json         端口等设置（默认端口 172，被占用会自动向后顺延）
├─ data\
│  ├─ providers.json   API 提供商列表（Base URL / Key / 拉取到的模型，本地保存）
│  ├─ user.json        默认模型等（本地保存）
│  ├─ canvas.json      画布分区与布局（自动保存）
│  ├─ history.json     生成历史
│  ├─ chats.json       GPT 对话窗口（自动保存）
│  ├─ chat-prompts.json 常用对话库（保存消息）
│  └─ prompts.json     常用提示词库（保存当前提示词）
├─ cache\
│  ├─ generated\       生成图缓存
│  └─ uploads\         上传的本地图片缓存
└─ public\             前端页面（画布 UI，含 images\icon.png 图标）
```

## 五、常见问题

- **端口 172 被占用**：程序会自动尝试 173~191，并打印实际地址；**全部被占用时自动改用系统随机可用端口**（保证一定能启动，以窗口里「浏览器地址」为准）。也可编辑 `config.json` 的 `port` 换端口，或用环境变量 `PORT` 指定。
- **报「鉴权失败 / 401」**：检查 API Key 是否复制完整、是否有余额。
- **报「接口不存在 / 404」**：Base URL 填错或模型名平台已下架，换一个模型试试。
- **报「上游服务错误（502/503/504）」**：平台瞬时繁忙，程序会**自动重试 2 次**（间隔 2s/4s），日志面板与黑色窗口可看到重试过程；仍失败请稍后再试或更换模型。
- **生成很慢**：大图与高峰期可能等待数分钟，生图区会显示耗时进度；程序对上游最长等待约 5 分钟。
- **负面提示词没生效**：负面提示词仅对部分模型（qwen / 豆包 / flux 系）生效，其余模型平台会忽略。
- **对话报 413（请求体过大）**：GRS 等平台的请求体上限约 32MB，多轮视觉对话附图太多太大时会触发——程序已做两层处理：**在 GRS 下新插入的附图自动压缩到 2048px 以内**，发送时若仍超限会**自动剥除历史消息的附图（保留最新一张）**；仍报错请减少附图数量。
- **对话模型列表拉不到**：平台未开放 `/v1/models` 时勾选列表为空，窗口模型框会变成可手动输入的文本框，直接输入模型名即可。
- **MJ 出图慢/回传失败**：图片下载**只走平台 CDN 直链**（不再等待慢速的整图代理地址）；**四宫格直接逐张回传 4 张单图（不拼整图）**，U/V 等操作按钮自动挂在对应象限的图上；等待上限 10 分钟；若来源是 discord 域名，把「图片通道」切到 **转发加速 relay**；点右上角任务状态看**服务端接入日志**定位具体环节。MJ 生图/操作要在支持 MJ 的提供商下进行（如 comfly，已在设置里配好「MJ 接口地址」；GRS/momo 等不支持 MJ 会报 404/405）。
- **想停止服务**：关闭黑色窗口即可。

## 六、更新计划（后续版本）

- Midjourney（/mj）全流程、异步任务可视化管理
- 画布框选、多选、对齐辅助线与缩略图导航
- 节点连线式工作流（生成→编辑→再生成 链式自动执行）
- 对话窗口导出 Markdown / 角色预设（System Prompt）
